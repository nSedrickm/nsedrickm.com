import Image from "next/image";
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

const Home = () => {
  return (
    <div className="grid place-items-center ">
      <div className="text-8xl">nsedrickm</div>
    </div>
  );
};

export default Home;
